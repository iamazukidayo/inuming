<%= form_with model: [post, comment], local: false do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, rows: '5', class: 'form-control', placeholder: "コメントをここに", id: "comment_textarea" %>
  </div>
  <%= f.submit "送信する", class: "btn btn-sm btn-success" %>
 <% end %>
